import {campaignResolvers, campaignTypeDefs } from "./campaign"


const typeDefs = [
    campaignTypeDefs
]

const resolvers = [
    campaignResolvers
]

export {
    typeDefs,
    resolvers
}
